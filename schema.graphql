type Transfer @entity {
  id: ID!
  amount: BigInt!
  token: Bytes! # address
  to: Bytes! # address
  txHash: String!
}

type Deposit @entity {
  id: ID!
  amount: BigInt!
  token: Bytes! # address
  from: Bytes! # address
  txHash: String!
}
